"""from app_quiz.models import QuizModel


class QuizSearchAdapter:

    @staticmethod
    def search(query_string):
        # Sua lógica de busca vai aqui. Por exemplo:
        results = QuizModel.objects.filter(title__icontains=query_string)
        return results"""
